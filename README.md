# fake-news-detector
his project detects fake and misleading news articles by converting raw text into numerical features using TF-IDF and classifying them with a Passive-Aggressive Classifier. It handles data preprocessing, model training, evaluation with a confusion matrix, and achieves over 92% accuracy on benchmark test data.
